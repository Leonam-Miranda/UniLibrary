<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.UniLibrary" parent="Theme.Material3.Light.NoActionBar">
        <!-- Cores Principais -->
        <item name="colorPrimary">@color/primary_red</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">#FDEDEB</item>
        <item name="colorOnPrimaryContainer">@color/primary_red</item>

        <!-- Cores de Fundo e Superfície -->
        <item name="android:windowBackground">@color/white</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">#212121</item>
        <item name="colorOutline">#E0E0E0</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.UniLibrary" parent="Base.Theme.UniLibrary" />
</resources>